{
  "name": "space-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "Personal space dashboard — stargazer conditions + 3D solar system overview",
  "scripts": {
    "dev": "wrangler pages dev public",
    "deploy": "wrangler pages deploy public"
  },
  "devDependencies": {
    "wrangler": "^3.91.0"
  }
}
